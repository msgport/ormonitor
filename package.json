{
  "name": "ormonitor",
  "private": true,
  "version": "1.0.0",
  "description": "Monitor for ORMP",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace server dev\" \"yarn workspace client start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
